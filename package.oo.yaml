version: 0.0.13
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: pnpm install
icon: "./icon.png"
dependencies:
  oomol-transform: 0.0.5
name: oomol-file
displayName: File
description: Related to operation files.
releaseNotes: fix list block bug
repository: https://github.com/oomol-flows/oomol-file
